version: "3"

services:
  test1:
    image: busybox:1.36
    container_name: test1
    ports:
      - "{{ Value.out_port }}:{{ Value.in_port }}"
    environment:
      - PORT=8080
      - DOCKER_MYSQL={{ Docker.ps_inspect('iflyssemysql') }}
    command: {{ Value.cmd }}
